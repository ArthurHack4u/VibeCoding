// Clase que simula una caseta (taquilla) en cada entrada del puente.
class Caseta {
    // Registra el pago recibido y lo retorna (aquí se podría expandir para incluir registro de datos).
    public double registrarPago(double monto) {
        // Se podría agregar lógica de atención o registro
        return monto;
    }
}

